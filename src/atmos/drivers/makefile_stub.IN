################################################################################
################################################################################
# 
#     Makefile for Global NMM Model 
#
#     Use:
#     make         -  build the executable
#     make clean   -  start with a clean slate
#
#     The following macros will be of interest:
#
#         TARGET   - name of the executable
#         CPP      - name of CPP
#         ARCH     - architecture
#         CPPFLAGS - CPP flags
#         OPTS     - compiler code optimizations
#         LIST     - source listing
#         SMP      - threading
#         TRAPS    - runtime traps for floating point exceptions
#         PROFILE  - source code profiling ( -pg )
#         DEBUG    - -g
#         MEM      - user data area and stack size
#         MAP      - load map
#         W3LIB    - w3lib
#         ESSL     - ESSL library
#         MASS     - MASS library
#         TRACE    - MPI tracing
#
#################################################################################

# Define the name of the executable
#
#
# CPP, Compiler, and Linker Options
#
#
include ../../../build/compile_settings
MAKEFILE=makefile_stub
TARGET= atmos__NEMSCORE__stub.a

MODULE=        \
	_STUBMODULES_

.SUFFIXES:	.F90 .f90 .o

.F90.f90:
	$(CPP) $(CPPFLAGS) $< > $*.f90

default:
	@gmake -f $(MAKEFILE) all

all: $(LIBDIR)/libstub/$(TARGET)

$(LIBDIR)/libstub/$(TARGET): $(MODULE) 
	$(AR) $(ARFLAGS) $@ $?

$(MODULE): %.o: %.f90 $(DEPS)
	$(F90) $(FFLAGSDB_STUB) -c $*.f90

clean:	
	/bin/rm -f  $(LIBDIR)/libstub/$(TARGET) 
	/bin/rm -f  $(MODDIR)/modstub/_NEMSCORE_/*mod
	/bin/rm -f *.f90 
	/bin/rm -f *.lst 
	/bin/rm -f *.o 
	/bin/rm -f *.mod 
	/bin/rm -f lm 
	/bin/rm -f map
	/bin/rm -f makefile_stub
