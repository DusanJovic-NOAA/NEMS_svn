####   requires necessary grid components
#
#
include ../../build/compile_settings
# *****************************************************************
#
all: 
	_MODULES_
	echo "_NEMSCORE_ stub libraries created."
clean:
	_CLEANMODULES_
	rm -f $(MODDIR)/modstub/gfs/*
	rm -f $(MODDIR)/modstub/nmm/*
	rm -f $(LIBDIR)/libstub/*
	echo "_NEMSCORE_ stub libraries erased"
